# (조망) 전시 초기화 전체 흐름 — 세 외부 API가 전시 한 건을 완성하기까지

> 개별 기능 문서(06 동기화 · 07 장르 분류)를 **하나의 타임라인으로 합친 조망도**다. "전시 데이터는 어디서 오고, 언제, 몇 번 불러서 채워지는가"를 한 장으로 보기 위한 문서 — 리팩터링·개선 논의의 출발점.
>
> 📌 **기준**: `develop`(= `main`, 2026-07-15 역머지 완료 시점) 코드 기준이다. 3단계(영업시간·Google Maps)는 한때 main에만 있었으나(PR #114가 main에 직접 머지) 지금은 develop에도 반영돼 있다.

## 세 외부 API의 역할

| API | 채우는 값 | 호출 단위 | 실행당 상한 | 환경별 |
|---|---|---|---|---|
| 📋 공공데이터 (한눈에보는문화정보) | 전시 목록(제목·기간·장소·좌표) + 상세(가격·설명·주소) | 목록 = 페이지당 1콜 / 상세 = **전시당 1콜** | 목록 5페이지 × 100건 = 최대 500건 | 키(`CULTURE_API_KEY`) 없으면 0콜 스킵 |
| ✨ AI (Gemini) | 장르 키워드(마스터 10종 중 1개) | **배치당 1콜** (20건 묶음) | 3배치 = **최대 60건** | 운영=gemini / develop·로컬=random(무비용) |
| 📍 Google Maps (Places New) | 영업시간(운영시간) | **장소당 1콜** (같은 주소 전시는 묶어서) | 최대 100장소 | 운영=google / 그 외=mock(0콜) |

## 전체 흐름

```mermaid
sequenceDiagram
    autonumber
    participant 트리거 as 부팅 / 매일 자정
    participant 서버 as 여운 서버
    participant DB as 전시 DB
    participant 공공 as 공공데이터 API
    participant AI as AI API (Gemini)
    participant 구글 as Google Maps API

    rect rgb(235, 245, 255)
    Note over 트리거, 공공: 1단계 — 전시 목록·상세 수집 【부팅 O / 자정 O】
    트리거->>서버: 동기화 시작
    Note right of 트리거: 부팅 시엔 데몬 스레드 — 서버 기동(헬스체크)을 막지 않는다

    loop 페이지 1~5 (100건씩)
        서버->>공공: 전시 목록 요청
        공공-->>서버: 전시 목록 (최대 500건)
    end
    Note right of 공공: 인증키 없으면 외부 호출 0 — 여기서 끝

    loop 수집한 전시 한 건씩
        서버->>DB: 이 전시 이미 있나? (externalId로 확인)
        alt 이미 있고 상세까지 채워진 전시
            서버->>서버: 건너뜀
            Note right of 서버: 상세 API 호출 0 — 매일 돌아도 공짜인 이유
        else 신규 전시 · 또는 상세가 비어 있는 기존 전시
            서버->>공공: 상세 요청 (가격·설명·주소)
            alt 상세 있음
                공공-->>서버: 상세 데이터
            else 원천에 상세 없음
                공공-->>서버: 빈 응답 → "확인함"만 표기
                Note right of 서버: 값은 못 채워도 재조회 대상에서 빼 매일 반복 호출 방지
            end
            서버->>DB: 저장 (전시 1건 = 트랜잭션 1개)
        end
        Note right of 서버: 상세 호출이 일시 실패하면 이 전시만 연기 — 배치는 계속
    end
    end

    rect rgb(240, 255, 240)
    Note over 트리거, AI: 2단계 — 장르 분류 【부팅 O / 자정 O】 · 방금 들어온 신규분만
    loop 최대 3배치
        서버->>DB: 장르 없는 전시 20건 주세요
        alt 없음
            DB-->>서버: 0건 → 즉시 종료 (AI 호출 0)
        else 있음
            DB-->>서버: 전시 20건
            서버->>AI: 20건 한 번에 분류해줘 (배치 1콜)
            alt 정상
                AI-->>서버: 장르 20개
            else 키 없음 · 429 한도 · 오류
                AI-->>서버: 실패 → 랜덤 장르로 폴백
                Note right of AI: 등록·동기화 흐름은 절대 안 깨진다(부가 기능)
            end
            서버->>DB: 장르 저장
        end
    end
    Note right of 서버: (!) 실행당 최대 60건 — 초기 수백 건이면 전량 분류까지 며칠
    end

    rect rgb(255, 248, 235)
    Note over 트리거, 구글: 3단계 — 영업시간 【부팅 X / 자정만】
    Note right of 트리거: 부팅에서 뺀 이유 — 재시작할 때마다 유료 호출이 재발하지 않도록

    서버->>DB: 영업시간 원본 스테이징 전체 삭제 (매 실행 초기화)
    서버->>DB: 주소 있고 · 미조회거나 30일 지난 전시 주세요
    DB-->>서버: 후보 전시들
    서버->>서버: 같은 주소끼리 묶기 → 최대 100장소
    Note right of 서버: 같은 장소의 전시 10건 = 1콜 (비용 절감 핵심)

    loop 장소마다
        서버->>구글: "장소명 + 주소"로 검색 (1콜)
        alt 찾음
            구글-->>서버: 요일별 영업시간
            서버->>서버: 우리 표시 형식으로 변환
            서버->>DB: 원본 스테이징 + 그 장소 전시들에 영업시간 저장
        else 못 찾음
            구글-->>서버: 결과 없음
            서버->>DB: 값은 비우고 조회 시각만 기록 (재조회 백오프)
        end
        Note right of 서버: 전송 실패 시 이 장소만 스킵 — 조회 시각을 안 남겨 다음 주기 재시도
    end
    end

    Note over 트리거, 구글: 정상 상태(전부 채워짐)에선 세 API 호출이 모두 ≈ 0 — 신규·만료분만 처리
```

## 읽는 법 — 설계의 핵심 3가지

1. **"이미 채워진 건 안 건드린다"가 전 구간 원칙.** 상세는 `detailSyncedAt`, 장르는 `genreKeyword IS NULL`, 영업시간은 `synced_at + 30일`로 각각 대상을 거른다. 그래서 매일 자정에 돌아도 스테디 상태에선 외부 호출이 거의 0이고, 재실행해도 안전하다(멱등).
2. **보강은 전부 부가 기능 — 실패해도 본류를 안 깨뜨린다.** AI가 죽으면 랜덤 장르로, 구글이 죽으면 그 장소만 스킵, 상세가 실패하면 그 전시만 연기. 대신 **조용히 비어 있을 수 있다**는 뜻이기도 하다.
3. **호출 단위가 곧 비용 설계.** 상세만 전시당 1콜(무료라 가능)이고, 유료·한도 있는 AI와 구글은 각각 배치당·장소당 1콜로 묶었다.

## 개선 후보 (이 흐름에서 눈에 띄는 것)

| # | 논점 | 근거 |
|---|---|---|
| 1 | **장르 백필이 느리다** — 실행당 60건(20×3), 자정 하루 1회 → 초기 수백 건이면 전량 분류에 며칠 | `enrich.genre-max-batches-per-run: 3`. 설정 주석은 "소량씩 **주기(interval-ms)**에 나눠 드레인"이라 하지만 **그 주기 스케줄러는 이미 없다**(동기화 직후에만 실행) — 주석이 스테일이고, 드레인을 책임지던 경로가 사라진 상태 |
| 2 | **영업시간 원본을 매 실행 전부 지운다** | `resetPlaceHoursSnapshots()`가 `google_place_hours`를 통째로 delete 후 시작 — 이력이 안 남고, 그 실행이 실패하면 직전 원본도 이미 사라진 뒤 |
| 3 | **수집 상한 500건** (100×5페이지) | `max-pages: 5` — 원천에 더 있어도 못 가져온다. 의도된 캡인지 확인 필요 |
| 4 | **재배포마다 목록 5콜 재수집** | 부팅 동기화가 항상 전체 목록을 다시 훑는다(상세는 미완성분만이라 저렴하지만, 목록은 매번) |
| 5 | ~~**브랜치 분기** — 영업시간이 main에만 있음~~ → **해소됨(2026-07-15 역머지)** | 재발 방지: main에 직접 머지한 뒤 develop 역머지를 빼먹으면 같은 일이 반복된다 |
