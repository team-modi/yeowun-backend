# 전시 목록·상세·필터 강화 + 방문기록 스냅샷 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 공공데이터(15138937) 전시를 1시간 배치 + 부팅 시 DB에 시딩하고, 목록(지역 필터·최신/종료/인기 정렬)·상세(소개·관람료·주소·이미지 지연 수집)를 DB 조회로 제공하며, "다녀온 전시"(records)에 전시 정보를 스냅샷으로 박제한다.

**Architecture:** A안(수집-적재 후 DB 조회). 목록은 `/realm2?realmCode=D000` 페이지 순회로 upsert, 상세는 `/detail2?seq=`를 최초 조회 시 지연 수집·캐시. 조회 경로는 외부 실시간 호출 없이 DB만 본다. 인기순은 우리 앱 자체 조회수(`our_view_count`).

**Tech Stack:** Java 21, Spring Boot 4.1.0, MySQL 8.4, JPA(Hibernate)+QueryDSL(현재 Spring Data JPA Specification 사용), Flyway, WebClient, jackson-dataformat-xml, Testcontainers-MySQL.

## Global Constraints

- 상태 변경은 Entity 메서드 안에서만. Facade/Service는 load·조율·save (CLAUDE.md 핵심).
- 성공 응답은 전부 200 (201/PATCH 금지). API prefix `/api/v1`, 리소스 복수형 케밥.
- Repository 3-클래스(포트 domain / JpaRepository infra / Impl infra). soft-delete 조회는 `deletedAt is null`.
- 다른 애그리거트엔 ID·값만 전달, 경계 넘는 @ManyToOne 금지.
- 검증: 생성·수정 후 `./gradlew build`(전 스위트) 통과. 테스트는 Testcontainers-MySQL(Docker 데몬 필요).
- 외부 소스 계약(실호출 검증 2026-07-02): base `https://apis.data.go.kr/B553457/cultureinfo`, 성공 `resultCode=00`, 응답 XML, 날짜 `YYYYMMDD`, 파라미터 `serviceKey`·`PageNo`(대문자 P)·`numOfrows`(소문자 r)·`realmCode=D000`(전시). 목록 item: `seq,title,startDate,endDate,place,realmName,area,sigungu,thumbnail,gpsX,gpsY,serviceName`. 상세(detail2): +`price,contents1,url,phone,imgUrl,placeUrl,placeAddr,placeSeq`. 작가·세부장르·운영시간 미제공.
- 키: `.env`의 `CULTURE_API_KEY`(data.go.kr 15138937 일반 인증키, 이미 설정·검증됨).
- **저장 정책(사용자 확정)**: API 응답 필드는 **전부 컬럼으로 영속**(누락 없음 — 나중에 필요해져도 재수집 불필요). **API 응답 DTO는 UI에 필요한 값만** 노출. 저장 ≠ 노출.
- 15138937 전체 필드(전부 저장 대상): 목록 `seq,title,startDate,endDate,place,realmName,area,sigungu,thumbnail,gpsX,gpsY,serviceName`; 상세 `+price,contents1,url,phone,imgUrl,placeUrl,placeAddr,placeSeq`.

---

## Phase 1 — 전시 도메인 A안 전환 + 소스 교체 (스펙 ②)

### Task 1: B 리팩토링 되돌리기 + 외부 설정 교체

**Files:**
- Revert(작업트리→HEAD): `src/main/java/modi/backend/application/exhibition/ExhibitionFacade.java`, `ExhibitionResult.java`, `domain/exhibition/CatalogExhibitionData.java`, `ExhibitionRepository.java`, `infra/exhibition/ExhibitionJpaRepository.java`, `ExhibitionRepositoryImpl.java`, `src/test/java/modi/backend/interfaces/ExhibitionIntegrationTest.java`, `.http/exhibition.http`
- Modify: `src/main/resources/application.yaml`, `src/main/java/modi/backend/config/PublicDataProperties.java`

**Interfaces:**
- Produces: A안 baseline(원 커밋 상태의 `ExhibitionFacade.search`=Specification 쿼리, `getDetail`=findById) + 새 base-url/파라미터 설정.

- [ ] **Step 1: B 변경 되돌리기 (커밋 전 작업트리 변경 폐기)**

Run:
```bash
cd "/Users/jins/Desktop/2026 Project/modi/backend"
git checkout -- \
  src/main/java/modi/backend/application/exhibition/ExhibitionFacade.java \
  src/main/java/modi/backend/application/exhibition/ExhibitionResult.java \
  src/main/java/modi/backend/domain/exhibition/CatalogExhibitionData.java \
  src/main/java/modi/backend/domain/exhibition/ExhibitionRepository.java \
  src/main/java/modi/backend/infra/exhibition/ExhibitionJpaRepository.java \
  src/main/java/modi/backend/infra/exhibition/ExhibitionRepositoryImpl.java \
  src/test/java/modi/backend/interfaces/ExhibitionIntegrationTest.java \
  .http/exhibition.http
```
Expected: 위 파일들이 HEAD(A안 원본)로 복귀. `git status`에서 사라짐. (docs/·compose.yaml·.env는 유지)

- [ ] **Step 2: application.yaml 소스 교체**

`src/main/resources/application.yaml`의 `app.public-data.culture` 블록을 교체:
```yaml
  public-data:
    culture:
      base-url: ${CULTURE_API_BASE_URL:https://apis.data.go.kr/B553457/cultureinfo}
      service-key: ${CULTURE_API_KEY:}
      realm-code: D000        # 전시
      num-of-rows: 100
      max-pages: 5
```

- [ ] **Step 3: PublicDataProperties에 realmCode 추가**

`PublicDataProperties`에 `String realmCode` 추가(기본 `D000`):
```java
@ConfigurationProperties(prefix = "app.public-data.culture")
public record PublicDataProperties(String baseUrl, String serviceKey, String realmCode,
        Integer numOfRows, Integer maxPages) {
    public PublicDataProperties {
        if (numOfRows == null || numOfRows <= 0) numOfRows = 100;
        if (maxPages == null || maxPages <= 0) maxPages = 5;
        if (realmCode == null || realmCode.isBlank()) realmCode = "D000";
    }
    public boolean isConfigured() {
        return serviceKey != null && !serviceKey.isBlank() && baseUrl != null && !baseUrl.isBlank();
    }
}
```

- [ ] **Step 4: 빌드 확인**

Run: `./gradlew compileJava`
Expected: BUILD SUCCESSFUL (A안 baseline + 새 설정 컴파일).

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "chore(exhibition): B 실시간 프록시 되돌리고 A안 baseline + 15138937 소스 설정으로 전환"
```

---

### Task 2: 외부 응답 파서 재작성 (XML 24필드 계약)

**Files:**
- Rewrite: `src/main/java/modi/backend/infra/exhibition/CultureApiResponse.java`
- Modify: `build.gradle` (jackson-dataformat-xml 의존 추가)
- Test: `src/test/java/modi/backend/infra/exhibition/CultureApiResponseTest.java`

**Interfaces:**
- Produces: `CultureApiResponse.isSuccess()`(resultCode "00"), `List<Item> items()`, `record Item(String seq,title,startDate,endDate,place,realmName,area,sigungu,thumbnail,gpsX,gpsY,serviceName, price,contents1,url,phone,imgUrl,placeUrl,placeAddr,placeSeq)`.

- [ ] **Step 1: XML 의존 추가**

`build.gradle` dependencies에 추가:
```groovy
	implementation 'com.fasterxml.jackson.dataformat:jackson-dataformat-xml'
```
Run: `./gradlew dependencies --configuration compileClasspath | grep jackson-dataformat-xml` → 존재 확인.

- [ ] **Step 2: 실패 테스트 작성 (파싱)**

`CultureApiResponseTest`:
```java
package modi.backend.infra.exhibition;

import static org.assertj.core.api.Assertions.assertThat;
import com.fasterxml.jackson.dataformat.xml.XmlMapper;
import org.junit.jupiter.api.Test;

class CultureApiResponseTest {
    private final XmlMapper xml = new XmlMapper();

    @Test
    void 성공응답_파싱() throws Exception {
        String body = "<response><header><resultCode>00</resultCode><resultMsg>정상입니다.</resultMsg></header>"
            + "<body><totalCount>266</totalCount><PageNo>1</PageNo><numOfrows>1</numOfrows><items>"
            + "<item><serviceName>전시</serviceName><seq>319005</seq><title>패트릭 블랑</title>"
            + "<startDate>20180616</startDate><endDate>20281231</endDate><place>부산현대미술관</place>"
            + "<realmName>전시</realmName><area>부산</area><sigungu>사하구</sigungu>"
            + "<thumbnail>http://t/x.jpg</thumbnail><gpsX>128.9</gpsX><gpsY>35.1</gpsY></item>"
            + "</items></body></response>";
        CultureApiResponse res = xml.readValue(body, CultureApiResponse.class);
        assertThat(res.isSuccess()).isTrue();
        assertThat(res.items()).hasSize(1);
        assertThat(res.items().get(0).seq()).isEqualTo("319005");
        assertThat(res.items().get(0).area()).isEqualTo("부산");
    }

    @Test
    void 에러응답_isSuccess_false() throws Exception {
        String body = "<response><header><resultCode>99</resultCode><resultMsg>ERR</resultMsg></header><body/></response>";
        assertThat(xml.readValue(body, CultureApiResponse.class).isSuccess()).isFalse();
    }
}
```

- [ ] **Step 3: 실패 확인**

Run: `./gradlew test --tests "modi.backend.infra.exhibition.CultureApiResponseTest"`
Expected: FAIL (필드/구조 불일치).

- [ ] **Step 4: CultureApiResponse 재작성**

```java
package modi.backend.infra.exhibition;

import java.util.List;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.dataformat.xml.annotation.JacksonXmlElementWrapper;
import com.fasterxml.jackson.dataformat.xml.annotation.JacksonXmlProperty;

/** 한눈에보는문화정보(15138937) realm2/detail2 XML 응답 매핑. */
@JsonIgnoreProperties(ignoreUnknown = true)
public record CultureApiResponse(Header header, Body body) {

    @JsonIgnoreProperties(ignoreUnknown = true)
    public record Header(String resultCode, String resultMsg) {}

    @JsonIgnoreProperties(ignoreUnknown = true)
    public record Body(Integer totalCount,
            @JacksonXmlElementWrapper(localName = "items")
            @JacksonXmlProperty(localName = "item") List<Item> items) {}

    /** 목록(realm2) 12필드 + 상세(detail2) 확장필드(목록 응답에선 null). */
    @JsonIgnoreProperties(ignoreUnknown = true)
    public record Item(String seq, String title, String startDate, String endDate, String place,
            String realmName, String area, String sigungu, String thumbnail, String gpsX, String gpsY,
            String serviceName, String price, String contents1, String url, String phone, String imgUrl,
            String placeUrl, String placeAddr, String placeSeq) {}

    public boolean isSuccess() {
        return header != null && "00".equals(header.resultCode());
    }

    public List<Item> items() {
        return body == null || body.items() == null ? List.of() : body.items();
    }
}
```

- [ ] **Step 5: 통과 확인 + Commit**

Run: `./gradlew test --tests "modi.backend.infra.exhibition.CultureApiResponseTest"` → PASS
```bash
git add -A && git commit -m "feat(exhibition): 외부 응답 파서를 15138937 XML 24필드 계약으로 재작성"
```

---

### Task 3: 수집 데이터 확장 + 클라이언트 재작성 (realm2 목록 + detail2 상세)

**Files:**
- Modify: `src/main/java/modi/backend/domain/exhibition/CatalogExhibitionData.java` (sigungu 추가)
- Create: `src/main/java/modi/backend/domain/exhibition/CatalogDetailData.java`
- Modify: `src/main/java/modi/backend/domain/exhibition/ExhibitionCatalogClient.java` (fetchDetail 추가)
- Rewrite: `src/main/java/modi/backend/infra/exhibition/CultureExhibitionClient.java`
- Modify: WebClient 설정(문자열 응답 사용하므로 XmlMapper로 파싱)
- Test: `src/test/java/modi/backend/infra/exhibition/CultureExhibitionClientTest.java` (MockWebServer)

**Interfaces:**
- Consumes: `CultureApiResponse`(Task 2), `PublicDataProperties`(Task 1).
- Produces: `ExhibitionCatalogClient.fetchAll(): List<CatalogExhibitionData>`, `fetchDetail(String externalId): Optional<CatalogDetailData>`. **API 필드 전부 담는다(누락 금지).**
  - `CatalogExhibitionData(externalId,title,place,startDate,endDate,region,category,posterUrl,detailUrl,serviceName,gpsX,gpsY, sigungu, realmName, areaText)` — `realmName`=원문, `areaText`=area 원문, `region`=area 파생 enum, `category`=realmName 파생.
  - `CatalogDetailData(price, description, detailUrl, phone, imgUrl, placeUrl, placeAddr, placeSeq)`

- [ ] **Step 1: CatalogExhibitionData에 필드 추가(전 API 필드 저장)**

record 끝에 `String sigungu, String realmName, String areaText` 파라미터 추가(기존 필드 뒤). `region`은 area 파생 enum, `areaText`는 area 원문, `realmName`은 분야 원문. `isPersistable()` 유지(externalId·title 기준).

- [ ] **Step 2: CatalogDetailData 생성**

```java
package modi.backend.domain.exhibition;

/** detail2 지연 수집 필드. 목록엔 없고 상세 진입 시 채운다. 결측 잦아 전부 nullable. */
public record CatalogDetailData(String price, String description, String detailUrl, String phone,
        String imgUrl, String placeUrl, String placeAddr, String placeSeq) {
}
```

- [ ] **Step 3: 포트에 fetchDetail 추가**

`ExhibitionCatalogClient`:
```java
List<CatalogExhibitionData> fetchAll();
java.util.Optional<CatalogDetailData> fetchDetail(String externalId);
```

- [ ] **Step 4: 실패 테스트 (MockWebServer로 realm2/detail2)**

`CultureExhibitionClientTest` — okhttp3 `MockWebServer`로 base-url을 목 서버로 돌리고 realm2/detail2 XML 응답을 큐잉해 `fetchAll()`·`fetchDetail("319005")` 결과를 검증한다. (자세한 XML은 Task 2 샘플 재사용, detail2는 `<price>무료</price><placeAddr>부산광역시 사하구 낙동남로 1191</placeAddr>` 포함.)
- 검증: `fetchAll()` 1건, `.area→region=BUSAN`, `.sigungu="사하구"`; `fetchDetail("319005").get().placeAddr()`="부산광역시 사하구 낙동남로 1191".

Run: `./gradlew test --tests "*CultureExhibitionClientTest"` → FAIL.

- [ ] **Step 5: CultureExhibitionClient 재작성**

핵심 로직(문자열 응답 → XmlMapper 파싱, realm2 페이지 순회, detail2 단건):
```java
@Override
public List<CatalogExhibitionData> fetchAll() {
    if (!properties.isConfigured()) { log.info("CULTURE_API_KEY 미설정 — 동기화 스킵"); return List.of(); }
    List<CatalogExhibitionData> collected = new ArrayList<>();
    for (int pageNo = 1; pageNo <= properties.maxPages(); pageNo++) {
        List<CultureApiResponse.Item> items = call("/realm2", uri -> uri
                .queryParam("serviceKey", properties.serviceKey())
                .queryParam("PageNo", pageNo)
                .queryParam("numOfrows", properties.numOfRows())
                .queryParam("realmCode", properties.realmCode()));
        items.stream().map(this::toData).filter(CatalogExhibitionData::isPersistable).forEach(collected::add);
        if (items.size() < properties.numOfRows()) break;
    }
    return collected;
}

@Override
public Optional<CatalogDetailData> fetchDetail(String externalId) {
    if (!properties.isConfigured()) return Optional.empty();
    List<CultureApiResponse.Item> items = call("/detail2", uri -> uri
            .queryParam("serviceKey", properties.serviceKey())
            .queryParam("seq", externalId));
    return items.stream().findFirst().map(i -> new CatalogDetailData(
            blankToNull(i.price()), blankToNull(i.contents1()), blankToNull(i.url()), blankToNull(i.phone()),
            blankToNull(i.imgUrl()), blankToNull(i.placeUrl()), blankToNull(i.placeAddr()), blankToNull(i.placeSeq())));
}

private List<CultureApiResponse.Item> call(String path, Function<UriBuilder, URI> query) {
    try {
        String xml = cultureWebClient.get().uri(uri -> query.apply(uri.path(path)))
                .retrieve().bodyToMono(String.class).block();
        CultureApiResponse body = xmlMapper.readValue(xml, CultureApiResponse.class);
        if (!body.isSuccess()) throw new CoreException(ExhibitionErrorCode.EXTERNAL_API_UNAVAILABLE,
                "외부 전시 API 비정상: " + (body.header() == null ? "null" : body.header().resultCode()));
        return body.items();
    } catch (CoreException e) { throw e;
    } catch (Exception e) {
        log.warn("외부 전시 API 호출 실패 {}: {}", path, e.getMessage());
        throw new CoreException(ExhibitionErrorCode.EXTERNAL_API_UNAVAILABLE, "외부 전시 API 호출 실패", e);
    }
}
```
- `toData`: 기존 매핑 + 마지막 인자로 `blankToNull(item.sigungu()), blankToNull(item.realmName()), blankToNull(item.area())`(sigungu·realmName 원문·areaText 원문). `region`은 `ExhibitionRegion.fromAreaText(item.area())`, `category`는 `ExhibitionCategory.fromRealmName(item.realmName())` 유지. 날짜 파싱은 기존 YYYYMMDD 로직 유지.
- `xmlMapper`는 `new XmlMapper()` 필드 상수. `cultureWebClient`는 base-url만 설정(기존 유지, .block() 사용).

- [ ] **Step 6: 통과 확인 + Commit**

Run: `./gradlew test --tests "*CultureExhibitionClientTest" "*CultureApiResponseTest"` → PASS
```bash
git add -A && git commit -m "feat(exhibition): realm2 목록·detail2 상세 클라이언트로 재작성(+sigungu)"
```

---

### Task 4: 지역 enum 확장 + area 매핑 재작성

**Files:**
- Modify: `src/main/java/modi/backend/domain/exhibition/ExhibitionRegion.java`
- Test: `src/test/java/modi/backend/domain/exhibition/ExhibitionRegionTest.java`

**Interfaces:**
- Produces: `ExhibitionRegion` = {SEOUL,GYEONGGI,INCHEON,DAEGU,GYEONGBUK,BUSAN,ULSAN,GYEONGNAM,SEJONG,JEONNAM,JEONBUK,JEJU,CHUNGNAM,CHUNGBUK,ETC}; `from(String)`, `fromAreaText(String)`.

- [ ] **Step 1: 실패 테스트**

```java
@Test void 지역텍스트_매핑() {
    assertThat(ExhibitionRegion.fromAreaText("부산")).isEqualTo(ExhibitionRegion.BUSAN);
    assertThat(ExhibitionRegion.fromAreaText("경기")).isEqualTo(ExhibitionRegion.GYEONGGI);
    assertThat(ExhibitionRegion.fromAreaText("세종특별자치시")).isEqualTo(ExhibitionRegion.SEJONG);
    assertThat(ExhibitionRegion.fromAreaText("광주")).isEqualTo(ExhibitionRegion.ETC); // 칩에 없음
    assertThat(ExhibitionRegion.fromAreaText(null)).isEqualTo(ExhibitionRegion.ETC);
}
```
Run → FAIL.

- [ ] **Step 2: enum 확장 + fromAreaText 재작성**

enum 상수를 위 15개로 확장. `fromAreaText`는 `area` 텍스트 startsWith 매핑(서울→SEOUL, 경기→GYEONGGI, 인천→INCHEON, 대구→DAEGU, 경북/경상북→GYEONGBUK, 부산→BUSAN, 울산→ULSAN, 경남/경상남→GYEONGNAM, 세종→SEJONG, 전남/전라남→JEONNAM, 전북/전라북→JEONBUK, 제주→JEJU, 충남/충청남→CHUNGNAM, 충북/충청북→CHUNGBUK, 그 외→ETC). `from(code)`는 valueOf 유지.

- [ ] **Step 3: 통과 + Commit**

Run: `./gradlew test --tests "*ExhibitionRegionTest"` → PASS
```bash
git add -A && git commit -m "feat(exhibition): 지역 enum 14개+ETC 확장, area 텍스트 매핑 재작성"
```

---

### Task 5: V7 마이그레이션 + Exhibition 엔티티 확장(지연상세·조회수)

**Files:**
- Create: `src/main/resources/db/migration/V7__add_exhibition_detail_and_views.sql`
- Modify: `src/main/java/modi/backend/domain/exhibition/Exhibition.java`
- Test: `src/test/java/modi/backend/domain/exhibition/ExhibitionTest.java` (기존에 추가)

**Interfaces:**
- Produces: `Exhibition.applyDetail(CatalogDetailData)`, `increaseView()`, getters `getSigungu/getPlaceAddr/getPhone/getPlaceUrl/getDetailSyncedAt/getOurViewCount`; `createCatalog(...)`·`refreshCatalog(...)`에 `sigungu` 인자 추가.

- [ ] **Step 1: V7 마이그레이션 (API 필드 전부 영속 — 저장 정책)**

```sql
-- API 응답 필드를 누락 없이 영속. 상세 지연수집 필드 + 우리 앱 조회수(인기순).
alter table exhibitions
    add column realm_name       varchar(50)   null,   -- realmName 원문(예 "전시")
    add column area_text        varchar(50)   null,   -- area 원문(region enum 파생 전 원본 보존)
    add column sigungu          varchar(50)   null,
    add column place_addr       varchar(500)  null,
    add column phone            varchar(100)  null,
    add column place_url        varchar(2048) null,
    add column img_url          varchar(2048) null,
    add column place_seq        varchar(100)  null,
    add column detail_synced_at datetime(6)   null,
    add column our_view_count   bigint not null default 0;
create index idx_exhibitions_view_count on exhibitions (our_view_count);
```
(기존 재사용: seq→external_id, contents1→description, price→price, url→detail_url, thumbnail→poster_url, serviceName→service_name, gpsX/Y→gps_x/y, area→region(파생)+area_text(원문). 즉 **API 12+8 필드를 하나도 안 버리고 저장**.)

- [ ] **Step 2: 실패 테스트 (엔티티 행위)**

`ExhibitionTest`에 추가:
```java
@Test void applyDetail_상세필드_채우고_동기화시각기록() {
    Exhibition e = Exhibition.createCatalog("S1","t","p",null,null,null,null,null, null); // 시그니처는 Step3 후
    e.applyDetail(new CatalogDetailData("무료","소개","http://u","010","http://img","http://pu","서울 어딘가"));
    assertThat(e.getPrice()).isEqualTo("무료");
    assertThat(e.getPlaceAddr()).isEqualTo("서울 어딘가");
    assertThat(e.getDetailSyncedAt()).isNotNull();
}
@Test void increaseView_조회수증가() {
    Exhibition e = Exhibition.createCatalog("S2","t","p",null,null,null,null,null,null);
    long before = e.getOurViewCount();
    e.increaseView();
    assertThat(e.getOurViewCount()).isEqualTo(before + 1);
}
```
Run → FAIL(컴파일).

- [ ] **Step 3: Exhibition 엔티티 확장**

필드 추가(`realmName, areaText, sigungu, placeAddr, phone, placeUrl, imgUrl, placeSeq, detailSyncedAt(LocalDateTime), ourViewCount(long, 기본 0)`). `createCatalog`/`refreshCatalog` 시그니처에 `sigungu, realmName, areaText` 인자 추가(전 API 필드 저장). 메서드:
```java
public void applyDetail(CatalogDetailData d) {
    this.price = d.price();
    this.description = d.description();
    if (d.detailUrl() != null) this.detailUrl = d.detailUrl();
    this.phone = d.phone();
    this.imgUrl = d.imgUrl();
    this.placeUrl = d.placeUrl();
    this.placeAddr = d.placeAddr();
    this.placeSeq = d.placeSeq();
    this.detailSyncedAt = LocalDateTime.now();
}
public boolean isDetailSynced() { return detailSyncedAt != null; }
public void increaseView() { this.ourViewCount += 1; }
```
`ourViewCount`는 `@Column(nullable=false)` `private long ourViewCount = 0;`.

- [ ] **Step 4: 통과 + Commit**

Run: `./gradlew test --tests "*ExhibitionTest"` → PASS
```bash
git add -A && git commit -m "feat(exhibition): V7 상세/조회수 컬럼 + applyDetail·increaseView 엔티티 행위"
```

---

### Task 6: syncCatalog 재작성 (realm2 → upsert, sigungu 포함)

**Files:**
- Modify: `src/main/java/modi/backend/application/exhibition/ExhibitionFacade.java`
- Test: `src/test/java/modi/backend/application/exhibition/ExhibitionSyncTest.java`

**Interfaces:**
- Consumes: `ExhibitionCatalogClient.fetchAll()`, `Exhibition.createCatalog/refreshCatalog(...,sigungu)`.
- Produces: `ExhibitionFacade.syncCatalog(): int`(적재/갱신 건수).

- [ ] **Step 1: 실패 테스트 (Mockito 단위)**

`fetchAll()` 목이 2건 반환 → `syncCatalog()` 후 `exhibitionRepository.save`가 2회, 반환 2. 같은 externalId 재수신 시 `refreshCatalog` 경로(findByExternalId present).
```java
@Test void syncCatalog_신규는_생성_기존은_갱신() {
    given(catalogClient.fetchAll()).willReturn(List.of(sample("S1"), sample("S2")));
    given(exhibitionRepository.findByExternalId("S1")).willReturn(Optional.empty());
    given(exhibitionRepository.findByExternalId("S2")).willReturn(Optional.of(existing("S2")));
    int n = facade.syncCatalog();
    assertThat(n).isEqualTo(2);
    verify(exhibitionRepository, times(2)).save(any());
}
```
Run → FAIL/조정.

- [ ] **Step 2: syncCatalog 구현(현행 유지 + sigungu 전달)**

기존 A안 `syncCatalog` 구조 유지하되 `create`/`refresh` 호출에 `data.sigungu()` 추가. `region`은 `ExhibitionRegion.fromAreaText`가 아니라 이미 `CatalogExhibitionData.region()`으로 매핑돼 옴(Task 3 toData). 반환은 `collected.size()`.

- [ ] **Step 3: 통과 + Commit**

Run: `./gradlew test --tests "*ExhibitionSyncTest"` → PASS
```bash
git add -A && git commit -m "feat(exhibition): syncCatalog를 realm2 수집분 upsert로(+sigungu)"
```

---

### Task 7: 스케줄러(매시) + 부팅 시딩

**Files:**
- Create: `src/main/java/modi/backend/application/exhibition/ExhibitionSyncScheduler.java`
- Modify: `src/main/java/modi/backend/BackendApplication.java` (또는 config에 `@EnableScheduling`)
- Modify: `src/main/java/modi/backend/application/exhibition/ExhibitionDemoSeeder.java` (부팅 시딩은 항상 syncCatalog 시도, 샘플은 빈 경우만)
- Test: `src/test/java/modi/backend/application/exhibition/ExhibitionSyncSchedulerTest.java`

**Interfaces:**
- Consumes: `ExhibitionFacade.syncCatalog()`.
- Produces: 매시 정각 자동 동기화 + 부팅 시 1회 동기화.

- [ ] **Step 1: @EnableScheduling 추가**

`BackendApplication`에 `@EnableScheduling` 애노테이션 추가(또는 `config/SchedulingConfig`).

- [ ] **Step 2: 스케줄러 작성**

```java
@Component
@RequiredArgsConstructor
public class ExhibitionSyncScheduler {
    private static final Logger log = LoggerFactory.getLogger(ExhibitionSyncScheduler.class);
    private final ExhibitionFacade exhibitionFacade;

    /** 매시 정각 공공데이터 전시 동기화. 실패해도 다음 주기에 재시도. */
    @Scheduled(cron = "0 0 * * * *")
    public void syncHourly() {
        try { log.info("전시 정기 동기화 {}건", exhibitionFacade.syncCatalog()); }
        catch (RuntimeException e) { log.warn("전시 정기 동기화 실패(다음 주기 재시도): {}", e.getMessage()); }
    }
}
```

- [ ] **Step 3: 부팅 시딩 항상 수행**

`ExhibitionDemoSeeder`의 `@ConditionalOnProperty(demo-seed)` 게이팅을 부팅 동기화에서 분리: 부팅 시 `syncCatalog()`는 항상 시도(키 없으면 내부에서 스킵), 샘플 표본 적재는 "카탈로그 비어있고 데모 플래그 on"일 때만. `ApplicationRunner`로 `syncCatalog()` 1회 호출 추가(cold start 방지).

- [ ] **Step 4: 테스트 (스케줄러가 facade 호출)**

Mockito로 `ExhibitionSyncScheduler.syncHourly()` 호출 시 `facade.syncCatalog()` 1회 호출·예외 삼킴 검증.
Run: `./gradlew test --tests "*ExhibitionSyncSchedulerTest"` → PASS.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(exhibition): 매시 스케줄러 + 부팅 시 동기화(cold start 방지)"
```

---

### Task 8: getDetail 지연 상세수집 + 조회수 증가

**Files:**
- Modify: `src/main/java/modi/backend/application/exhibition/ExhibitionFacade.java`
- Test: `src/test/java/modi/backend/application/exhibition/ExhibitionDetailTest.java`

**Interfaces:**
- Consumes: `ExhibitionRepository.findById`, `ExhibitionCatalogClient.fetchDetail`, `Exhibition.applyDetail/increaseView/isDetailSynced`.
- Produces: `getDetail`이 최초 CATALOG 조회 시 detail2 1회 수집·저장, 이후 캐시. 조회수 +1.

- [ ] **Step 1: 실패 테스트 (Mockito)**

```java
@Test void 상세_최초조회시_detail2수집_후_캐시_및_조회수증가() {
    Exhibition e = catalogNotSynced("S1"); // detailSyncedAt null
    given(exhibitionRepository.findById(1L)).willReturn(Optional.of(e));
    given(catalogClient.fetchDetail("S1")).willReturn(Optional.of(new CatalogDetailData("무료",null,null,null,null,null,"주소")));
    facade.getDetail(new ExhibitionCriteria.Detail(1L, null));
    verify(catalogClient).fetchDetail("S1");
    assertThat(e.getPlaceAddr()).isEqualTo("주소");
    assertThat(e.getOurViewCount()).isEqualTo(1);
    // 2번째 호출: 이미 synced → fetchDetail 추가 호출 없음
    facade.getDetail(new ExhibitionCriteria.Detail(1L, null));
    verify(catalogClient, times(1)).fetchDetail("S1");
    assertThat(e.getOurViewCount()).isEqualTo(2);
}
```
Run → FAIL.

- [ ] **Step 2: getDetail 구현**

```java
@Transactional
public ExhibitionResult.Detail getDetail(ExhibitionCriteria.Detail criteria) {
    Exhibition e = exhibitionRepository.findById(criteria.exhibitionId())
            .orElseThrow(() -> new CoreException(ExhibitionErrorCode.EXHIBITION_NOT_FOUND));
    if (!e.isAccessibleBy(criteria.requesterId()))
        throw new CoreException(ErrorType.FORBIDDEN, "타인의 개인 전시 접근: " + criteria.exhibitionId());
    if (e.isCatalog() && !e.isDetailSynced()) {
        try {
            catalogClient.fetchDetail(e.getExternalId()).ifPresent(e::applyDetail);
        } catch (CoreException ex) { /* 외부 실패 시 base만, 다음에 재시도 */ }
    }
    e.increaseView();
    exhibitionRepository.save(e);
    return ExhibitionResult.Detail.from(e);
}
```
- `@Transactional`(쓰기)로 변경(기존 readOnly 제거). `ExhibitionResult.Detail.from`에 새 필드(placeAddr/phone/imgUrl/viewCount) 매핑 추가는 Task 10에서.

- [ ] **Step 3: 통과 + Commit**

Run: `./gradlew test --tests "*ExhibitionDetailTest"` → PASS
```bash
git add -A && git commit -m "feat(exhibition): 상세 최초조회시 detail2 지연수집·캐시 + 조회수 증가"
```

---

### Task 9: 목록 정렬(최신/종료/인기) + 지역 필터

**Files:**
- Modify: `src/main/java/modi/backend/domain/exhibition/ExhibitionQuery.java` (sort 추가) 또는 Pageable Sort 사용
- Modify: `src/main/java/modi/backend/infra/exhibition/ExhibitionSpecifications.java` (변경 없음/지역 유지)
- Modify: `src/main/java/modi/backend/interfaces/exhibition/ExhibitionV1Controller.java` (sort 파라미터)
- Modify: `ExhibitionFacade.search` (sort → Pageable Sort 매핑)
- Test: `src/test/java/modi/backend/interfaces/ExhibitionIntegrationTest.java`

**Interfaces:**
- Produces: `GET /exhibitions?sort=latest|ending|popular`. latest=startDate DESC(기본), ending=endDate ASC, popular=ourViewCount DESC.

- [ ] **Step 1: 실패 테스트 (정렬)**

통합테스트에 3개: 목 3건(시작일·종료일·조회수 다르게 시드) → `sort=latest`·`ending`·`popular` 각각 첫 항목 검증.

- [ ] **Step 2: 정렬 매핑 구현**

Controller에서 `@RequestParam(defaultValue="latest") String sort`를 받아 Facade에 전달. Facade가 `sort`를 Spring `Sort`로 변환:
```java
private Sort toSort(String sort) {
    return switch (sort == null ? "latest" : sort) {
        case "ending" -> Sort.by(Sort.Direction.ASC, "endDate");
        case "popular" -> Sort.by(Sort.Direction.DESC, "ourViewCount");
        default -> Sort.by(Sort.Direction.DESC, "startDate");
    };
}
```
`PageRequest.of(pageable.getPageNumber(), pageable.getPageSize(), toSort(sort))`로 재구성해 `exhibitionRepository.search(query, pageable)`에 전달. 지역 필터는 기존 Specification 그대로.

- [ ] **Step 3: 통과 + Commit**

Run: `./gradlew test --tests "*ExhibitionIntegrationTest"` → PASS
```bash
git add -A && git commit -m "feat(exhibition): 목록 정렬 최신/종료/인기 + 지역 필터"
```

---

### Task 10: 응답 DTO 확장 (상세 주소·이미지·문의·조회수)

**Files:**
- Modify: `src/main/java/modi/backend/application/exhibition/ExhibitionResult.java`
- Modify: `src/main/java/modi/backend/interfaces/exhibition/dto/ExhibitionDto.java`
- Test: 통합테스트에서 상세 응답 필드 검증

**Interfaces:**
- Produces: `DetailResponse`에 `address(placeAddr)`, `imgUrl`, `phone`, `viewCount` 필드 추가(없으면 null). `ListItemResponse`는 기존 유지(+`sigungu` 선택).
- **저장 정책 준수**: DB엔 API 필드 전부(realm_name·area_text·place_seq 등) 저장돼 있지만, **응답 DTO는 UI 필요값만** 노출한다(realm_name·area_text·place_seq·place_url 등 내부 보존 필드는 응답에 넣지 않음). "필드 추가"는 화면에 쓰는 것만.

- [ ] **Step 1: Result.Detail 확장**

`ExhibitionResult.Detail`에 `placeAddr, imgUrl, phone, ourViewCount` 추가, `from(Exhibition)`에서 매핑.

- [ ] **Step 2: DetailResponse 확장 + 실패 테스트→통과**

`ExhibitionDto.DetailResponse`에 `address, imgUrl, phone, viewCount` 추가. 통합테스트: 상세 조회 응답에 `$.data.address` 등 존재. 없는 값은 null(일관 스키마).

- [ ] **Step 3: 통과 + Commit**

Run: `./gradlew test --tests "*ExhibitionIntegrationTest"` → PASS
```bash
git add -A && git commit -m "feat(exhibition): 상세 응답에 주소·이미지·문의·조회수 필드 추가"
```

---

## Phase 2 — 다녀온 전시(방문기록) 스냅샷 (스펙 ①)

### Task 11: ExhibitionSnapshot VO + V8 마이그레이션 + Record 엔티티

**Files:**
- Create: `src/main/java/modi/backend/domain/record/ExhibitionSnapshot.java`
- Create: `src/main/resources/db/migration/V8__add_record_exhibition_snapshot.sql`
- Modify: `src/main/java/modi/backend/domain/record/Record.java`
- Test: `src/test/java/modi/backend/domain/record/RecordTest.java` (신규/추가)

**Interfaces:**
- Produces: `ExhibitionSnapshot(String title, type, posterUrl, place, region, category, LocalDate startDate, endDate)`; `Record.create(...)`에 `ExhibitionSnapshot snapshot` 인자 추가; getters.

- [ ] **Step 1: ExhibitionSnapshot VO**

```java
package modi.backend.domain.record;

import java.time.LocalDate;
import modi.backend.support.error.CoreException;
import modi.backend.support.error.ErrorType;

/** 기록 생성 시점의 전시 표시정보 박제(문자열 — 전시 enum 비의존). */
public record ExhibitionSnapshot(String title, String type, String posterUrl, String place,
        String region, String category, LocalDate startDate, LocalDate endDate) {
    public ExhibitionSnapshot {
        if (title == null || title.isBlank())
            throw new CoreException(ErrorType.INVALID_INPUT, "전시 제목 스냅샷은 필수입니다.");
    }
}
```

- [ ] **Step 2: V8 마이그레이션**

```sql
alter table records
    add column exhibition_title      varchar(100)  not null,
    add column exhibition_type       varchar(20)   not null,
    add column exhibition_poster_url varchar(2048) null,
    add column exhibition_place      varchar(200)  null,
    add column exhibition_region     varchar(20)   null,
    add column exhibition_category   varchar(20)   null,
    add column exhibition_start_date date          null,
    add column exhibition_end_date   date          null;
```
> 기존 records 데이터 없다는 전제(개발단계). 있으면 default/backfill 선행.

- [ ] **Step 3: 실패 테스트 (Record.create가 스냅샷 저장)**

```java
@Test void create_전시스냅샷_저장() {
    var snap = new ExhibitionSnapshot("모네전","CATALOG","http://p","예술의전당","SEOUL","PAINTING", LocalDate.now(), null);
    Record r = Record.create(1L, 10L, snap, WriteMode.DIRECT, LocalDate.now(), "감상", null, null, null, AiStatus.PENDING);
    assertThat(r.getExhibitionTitle()).isEqualTo("모네전");
    assertThat(r.getExhibitionRegion()).isEqualTo("SEOUL");
}
```
Run → FAIL.

- [ ] **Step 4: Record 엔티티에 스냅샷 필드/파라미터 추가**

`Record`에 8개 스냅샷 컬럼 필드 + getters 추가. `create(...)` 시그니처에 `ExhibitionSnapshot snapshot` 추가(생성자에서 언팩). `replaceContent`(수정)는 스냅샷 미변경(불변).

- [ ] **Step 5: 통과 + Commit**

Run: `./gradlew test --tests "*RecordTest"` → PASS
```bash
git add -A && git commit -m "feat(record): 전시 스냅샷 VO + V8 컬럼 + Record.create 스냅샷 박제"
```

---

### Task 12: RecordService.create — 전시 조회 후 스냅샷 해석

**Files:**
- Modify: `src/main/java/modi/backend/application/record/RecordService.java`
- Test: `src/test/java/modi/backend/application/record/RecordServiceTest.java`

**Interfaces:**
- Consumes: `ExhibitionFacade.getDetail(ExhibitionCriteria.Detail(exhibitionId, userId)) → ExhibitionResult.Detail`.
- Produces: `RecordService.create`가 전시를 조회해 `ExhibitionSnapshot`으로 박제. 전시 미해결 → `EXHIBITION_NOT_FOUND`(404) 전파, 타인 CUSTOM → `FORBIDDEN`(403).

- [ ] **Step 1: 실패 테스트 (Mockito)**

```java
@Test void create_전시조회해_스냅샷박제() {
    given(exhibitionFacade.getDetail(any())).willReturn(detailResult("모네전","CATALOG","SEOUL"));
    var res = service.create(1L, createReq(10L));
    ArgumentCaptor<Record> cap = ArgumentCaptor.forClass(Record.class);
    verify(recordRepository).save(cap.capture());
    assertThat(cap.getValue().getExhibitionTitle()).isEqualTo("모네전");
}
@Test void create_전시없으면_404전파() {
    given(exhibitionFacade.getDetail(any())).willThrow(new CoreException(ExhibitionErrorCode.EXHIBITION_NOT_FOUND));
    assertThatThrownBy(() -> service.create(1L, createReq(999L))).isInstanceOf(CoreException.class);
    verify(recordRepository, never()).save(any());
}
```
Run → FAIL.

- [ ] **Step 2: 구현**

`RecordService`에 `ExhibitionFacade exhibitionFacade` 주입. `create`에서:
```java
var d = exhibitionFacade.getDetail(new ExhibitionCriteria.Detail(request.exhibitionId(), userId));
var snapshot = new ExhibitionSnapshot(d.title(), d.type(), d.posterUrl(), d.place(),
        d.region(), d.category(), d.startDate(), d.endDate());
Record record = Record.create(userId, request.exhibitionId(), snapshot, request.writeMode(),
        viewedAt, request.content(), request.aiSummary(), request.representativeEmotion(),
        request.cardPhrase(), aiStatus);
```
(나머지 replaceEmotions/Keywords/Media 로직은 기존 유지.)

- [ ] **Step 3: 통과 + Commit**

Run: `./gradlew test --tests "*RecordServiceTest"` → PASS
```bash
git add -A && git commit -m "feat(record): 기록 생성 시 전시 조회해 스냅샷 박제(404/403 전파)"
```

---

### Task 13: 방문목록 엔드포인트 + 응답 스냅샷 노출

**Files:**
- Modify: `src/main/java/modi/backend/interfaces/record/RecordV1Controller.java` (GET /records/exhibitions/visited)
- Modify: `src/main/java/modi/backend/interfaces/record/dto/RecordListItemResponse.java`, `RecordDetailResponse.java` (스냅샷 필드)
- Modify: `src/main/java/modi/backend/application/record/RecordService.java` (toListItem/toDetail 매핑)
- Test: `src/test/java/modi/backend/interfaces/record/RecordV1ControllerTest.java`

**Interfaces:**
- Produces: `GET /api/v1/records/exhibitions/visited` = 기존 `search`와 동일 파라미터/응답(내가 다녀온 전시 목록). 응답에 `exhibitionTitle, exhibitionPosterUrl, exhibitionPlace, exhibitionRegion, exhibitionCategory, exhibitionStartDate, exhibitionEndDate` 포함.

- [ ] **Step 1: 실패 테스트 (컨트롤러)**

`@WebMvcTest`로 `GET /api/v1/records/exhibitions/visited` 200 + `$.data.content[0].exhibitionTitle` 존재. (RecordService 목)

- [ ] **Step 2: DTO에 스냅샷 필드 추가 + 매핑**

`RecordListItemResponse`·`RecordDetailResponse`에 스냅샷 7필드 추가. `RecordService.toListItem/toDetail`에서 `record.getExhibitionTitle()` 등 매핑.

- [ ] **Step 3: 엔드포인트 추가**

`RecordV1Controller`에 `@GetMapping("/exhibitions/visited")` 추가 — 기존 `search(...)` 위임(동일 파라미터). Swagger `@Operation(summary="내가 다녀온 전시 목록")`.

- [ ] **Step 4: 통과 + Commit**

Run: `./gradlew test --tests "*RecordV1ControllerTest"` → PASS
```bash
git add -A && git commit -m "feat(record): 다녀온 전시 목록 엔드포인트 + 응답 전시 스냅샷 노출"
```

---

### Task 14: end-to-end 통합 테스트 + 문서/.http 갱신

**Files:**
- Modify: `src/test/java/modi/backend/interfaces/ExhibitionIntegrationTest.java`, `record/RecordV1ControllerTest.java`
- Create/Modify: `.http/exhibition.http`, `.http/record.http`(있으면)
- Modify: `docs/04_전시_구현.md` (A안 확정·소스 15138937·스케줄 반영)

**Interfaces:**
- Produces: 전시 동기화(목)→목록/상세(지연수집·조회수)→기록 작성(스냅샷)→다녀온 목록(스냅샷 노출) 전 흐름 검증.

- [ ] **Step 1: 통합 시나리오 테스트**

`ExhibitionCatalogClient` 목으로 realm2 표본 주입 → `syncCatalog` → `GET /exhibitions`(정렬·지역) → `GET /exhibitions/{id}`(detail2 목 → 주소/조회수) → 로그인 → `POST /records`(스냅샷 박제) → `GET /records/exhibitions/visited`(전시 제목·포스터 스냅샷 확인). CATALOG 목을 빈 목록으로 바꿔도 다녀온 목록 스냅샷 유지 확인.

- [ ] **Step 2: 전체 빌드**

Run: `./gradlew build`
Expected: BUILD SUCCESSFUL (전 스위트 그린).

- [ ] **Step 3: .http·docs 갱신**

`.http/exhibition.http`에 realm2 기반 예시·정렬·지역 필터·`/records/exhibitions/visited` 추가. `docs/04_전시_구현.md`에 A안 확정·소스 15138937·매시 스케줄·지연 상세수집 반영.

- [ ] **Step 4: 실앱 스모크(선택)**

`CULTURE_API_KEY` 설정 상태로 부팅 → `GET /api/v1/exhibitions` 실데이터 노출, 상세 1건 열어 주소·조회수 확인.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "test(exhibition,record): end-to-end 통합 + .http/문서 갱신"
```

---

## Phase 3 — HTML 데모 웹사이트 (정적, 백엔드 API 연동)

> 목업 기준 모바일 앱 스타일. `src/main/resources/static/`에 배치(Spring이 정적 서빙). 팔레트: 중립 그레이 + 블루 악센트(`#2f6bff` 계열), 카드/칩 라운드. 데이터 없는 필드(작가·태그·운영시간)는 **렌더 시 숨김**(일관 응답의 null 대응). 장르 칩은 데이터 없어 **비활성(회색, 클릭 불가)**.

### Task 15: 목록 화면 + 필터 바텀시트 (`exhibitions.html` 교체)

**Files:**
- Rewrite: `src/main/resources/static/exhibitions.html` (단일 파일 인라인 CSS/JS)

**Interfaces:**
- Consumes: `GET /api/v1/exhibitions?sort=&region=&page=&size=` → `ApiResponse<PageResponse<ListItemResponse>>`.

- [ ] **Step 1: 레이아웃·스타일 구현**
  - 모바일 프레임(max-width 430px, 중앙). 헤더(Logo + 북마크 아이콘). 검색 인풋(placeholder "전시를 검색해보세요").
  - 정렬 드롭다운(`최신순`=latest / `종료순`=ending / `인기순`=popular) + `필터` 버튼.
  - 카드 리스트: 썸네일(posterUrl, 없으면 회색 박스) + 제목 + 작가(없으면 생략) + `place/region` + `startDate ~ endDate`.
  - 하단 탭바(홈/전시정보/아카이브/프로필 — 데모는 홈만 활성).
- [ ] **Step 2: 데이터 로딩 JS**
  - fetch로 목록 조회, 정렬/지역 파라미터 반영, 카드 렌더. 카드 클릭 → `exhibition-detail.html?id={exhibitionId}` 이동.
- [ ] **Step 3: 필터 바텀시트**
  - `필터` 클릭 → 하단 시트 오픈. 지역 칩(전체·서울·경기·인천·대구·경북·부산·울산·경남·세종·전남·전북·제주·충남·충북). 장르 칩(드로잉…영화)은 **비활성 표시**. `초기화` / `{n}개 전시 보기`(선택 지역으로 재조회).
- [ ] **Step 4: 브라우저 확인**
  - `CULTURE_API_KEY` 설정 부팅 후 `http://localhost:8080/exhibitions.html` → 실데이터 카드 표시, 정렬·지역 필터 동작. (claude-in-chrome로 스크린샷 확인)
- [ ] **Step 5: Commit**
  ```bash
  git add -A && git commit -m "feat(web): 전시 목록·필터 데모 화면(exhibitions.html)"
  ```

### Task 16: 상세 화면 (`exhibition-detail.html`)

**Files:**
- Create: `src/main/resources/static/exhibition-detail.html`

**Interfaces:**
- Consumes: `GET /api/v1/exhibitions/{id}` → `ApiResponse<DetailResponse>`(title·posterUrl·startDate·endDate·place·region·price·description·address·imgUrl·phone·viewCount).

- [ ] **Step 1: 상세 레이아웃**
  - 상단 back + 북마크. 포스터(imgUrl 우선, 없으면 posterUrl, 없으면 회색). 제목. 작가(있으면). 태그(없으면 생략).
  - 정보 행: 전시 기간 / 장소(place) + 주소(address, 있으면) / 운영 시간(없으면 행 숨김) / 관람료(price) / 전시 소개(description).
  - 하단 고정 `기록하기` 버튼(데모: 알림 또는 방문기록 작성 안내 — 인증 필요 문구).
- [ ] **Step 2: 데이터 로딩 JS**
  - URL `?id=`로 상세 fetch, 필드 렌더. null 필드 행 숨김.
- [ ] **Step 3: 브라우저 확인 + Commit**
  - 목록→카드클릭→상세 이동·렌더 확인(claude-in-chrome).
  ```bash
  git add -A && git commit -m "feat(web): 전시 상세 데모 화면(exhibition-detail.html)"
  ```

---

## 실행 순서 · 의존

Phase 1(전시)이 Phase 2(기록 스냅샷)의 `ExhibitionFacade.getDetail` 계약(DB 기반, Result.Detail 필드)을 확정하고, Phase 3(HTML)는 Phase 1의 목록/상세 API에 의존하므로 **Task 1→16 순차**. 각 Task는 독립 테스트 사이클로 종료(리뷰 게이트).
